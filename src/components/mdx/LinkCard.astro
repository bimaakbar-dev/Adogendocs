---
import Icon from "@ui/Icons.astro";

interface Props {
  title?: string;
  href: string;
  icon?: string;
  description?: string;
}

const { 
    title, 
    href, 
    icon = "learn", 
    description 
} = Astro.props;

const isExternal = href.startsWith('http');
---

<a 
  href={href} 
  target={isExternal ? "_blank" : "_self"}
  rel={isExternal ? "noopener noreferrer" : ""}
  class="link-card group relative flex items-center justify-between gap-4 p-4 rounded-md border border-(--border-subtle) bg-(--bg-subtle) hover:bg-(--bg-muted) active:bg-(--bg-muted) transition-opacity duration-(--duration)"
>
  <div class="flex items-center gap-4">
    {icon && (
      <div class="inline-flex shrink-0 items-center justify-center">
        <Icon name={icon as any} class="w-5 h-5 text-(--fg-strong)" />
      </div>
    )}
    <div class="flex flex-col">
      <span class="text-body-16 font-medium text-(--fg-strong)">{title}</span>
      {description && (
        <span class="text-body-14 text-(--fg-muted) line-clamp-1 leading-relaxed">{description}</span>
      )}
    </div>
  </div>
  
  <div class="flex items-center justify-center shrink-0">
    <Icon 
      name={isExternal ? "external" : "arrow-right" as any} 
      class="w-4 h-4 text-(--fg-muted) group-hover:text-(--fg-strong) group-active:text-(--fg-strong) transition-opacity" 
    />
  </div>
</a>

<style is:global>
    .link-card {
        text-decoration: none !important;
    }
</style>