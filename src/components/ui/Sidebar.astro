---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className = '' } = Astro.props;
---

<div id={id} class={`flex h-screen flex-col scrollbar-hide py-2 ${className}`}>
  <slot />
</div>