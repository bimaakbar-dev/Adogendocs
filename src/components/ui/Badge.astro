---
// src/components/ui/Badge.astro
import type { HTMLAttributes } from 'astro/types';

type BadgeVariant = 'default' | 'accent' | 'success' | 'warning' | 'error';

interface Props extends HTMLAttributes<'span'> {
  variant?: BadgeVariant;
  class?: string;
}

const { 
  variant = 'default', 
  class: className, 
} = Astro.props;

const baseClasses = "inline-flex items-center px-2 py-0.5 text-sm font-medium rounded-full transition-colors";

const variantClasses: Record<BadgeVariant, string> = {
  default: "bg-(--bg-subtle) text-(--fg-strong) border border-(--border-subltle)/40",
  accent:  "bg-(--accent-soft) text-(--accent) border border-(--accent-soft)",
  success: "bg-(--success-soft) text-(--success) border border-(--success-soft)",
  warning: "bg-(--warning-soft) text-(--warning) border border-(--warning-soft)",
  error:   "bg-(--error-soft) text-(--error) border border-(--error-soft)",
};
---

<span class:list={[
    baseClasses,
    variantClasses[variant], 
    className]}>
  <slot />
</span>