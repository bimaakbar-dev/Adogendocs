---
interface Props {
    class?: string
}

const {
  class: className = '',
} = Astro.props;

const deafultClass = "min-h-screen flex-col pl-(--safe-left) pr-(--safe-right)";

const classes = [
    deafultClass,
    className,
]
---
<div class:list={[classes]}>
    <slot />
</div>
